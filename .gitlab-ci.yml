stages:
- build

build-core:
  stage: build
  script:
   - echo %USERDOMAIN%\%USERNAME%
   - SET
   - call .gitlab-scripts/init-deps-code.cmd
   - call .gitlab-scripts/build-projects-core.cmd
   - call .gitlab-scripts/test-projects-core.cmd
  tags:
    - windows

